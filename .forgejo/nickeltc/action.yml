name: "NickelTC"
description: "Toolchain for Kobo eReaders"
inputs:
  args:
    description: "The toolchain command to run"
    required: true
runs:
  using: "docker"
  image: "docker://strayrose/nickeltc:latest"
  args:
    - ${{ inputs.args }}
