query GetJournalQuotes($book_id: Int!, $user_id: Int!, $offset: Int!) {
  reading_journals(
    where: {
      event: { _eq: "quote" }
      book_id: { _eq: $book_id }
      user_id: { _eq: $user_id }
    }
    offset: $offset
    limit: 100
  ) {
    entry
    event
    id
    action_at
  }
}
